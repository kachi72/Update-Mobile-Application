<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Update" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Update" parent="Base.Theme.Update" />

    <style name="Theme.Update.Splash" parent="Theme.Update">
        <item name="android:windowSplashScreenBackground">@color/screen_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/app_icon_modern</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/screen_background</item>
        <item name="android:statusBarColor">@color/screen_background</item>
        <item name="android:navigationBarColor">@color/screen_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
